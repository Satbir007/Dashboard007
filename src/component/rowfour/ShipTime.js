import React from "react";
import DisplayInfoTwo from "../UI/DisplayInfoTwo";

const ShipTime = () => {
  const number = "2:32pm";
  const text = "PROJECT SHIP END TIME";
  return <DisplayInfoTwo number={number} text={text} />;
};

export default ShipTime;
